import * as React from 'react'

interface Props {
    
}

const allProjects: React.FC<Props> = (props: Props) => {
    return (
        <div>
            
        </div>
    )
}

export default allProjects
